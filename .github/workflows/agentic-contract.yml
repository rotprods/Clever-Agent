name: Agentic Contract

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate-agentic-state:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Validate canonical agentic state
        run: python scripts/validate_agentic_state.py
