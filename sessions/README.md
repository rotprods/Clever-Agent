# Session ContextPacks

Long or multi-agent sessions may persist recoverable ContextPacks under:

`<session-id>/CONTEXT.md`

Do not store secrets, raw credentials or unnecessary sensitive user data here.

The canonical project truth remains the root state/ledgers/evidence; session packs are recovery accelerators, not independent authority.
